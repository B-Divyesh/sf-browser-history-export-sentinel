[workspace]
members = ["cli"]
resolver = "2"

[workspace.package]
edition = "2024"
license = "MIT"
repository = "https://github.com/B-Divyesh/sf-browser-history-export-sentinel"

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
panic = "abort"
